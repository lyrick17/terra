Things to do:
    - create 2 webpages for users
    - 2 webpages for admins
    - erase entries button
    - cancel button
    - dapat mapupunta sa admin page if admin, sa member page if member
        - //$url = ($_SESSION['user-level'] === 1) ? "admin-home.php" : "user-home.php";